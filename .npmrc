# `legacy-peer-deps` a été retiré : les conflits de dépendances doivent
# remonter à l'installation plutôt que d'être acceptés en silence.
# C'est ce qui a permis de détecter que vite-react-ssg cible react-router 6.
engine-strict=false
